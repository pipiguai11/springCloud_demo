<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
           http://dubbo.apache.org/schema/dubbo
           http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!--
    只订阅，不注册服务（这里指的是不注册提供者信息，但是它的接口服务还是会暴露出去的，可以提供给消费者直连）
        为方便开发测试，经常会在线下共用一个所有服务可用的注册中心，
        这时，如果一个正在开发中的服务提供者注册，可能会影响消费者不能正常运行。

        可以让服务提供者开发方，只订阅服务(开发的服务可能依赖其它服务)，
        而不注册正在开发的服务，通过直连测试正在开发的服务。
     -->

    <dubbo:application name="dubbo-provider"/>
    <!-- 通过指定register属性为false禁用注册 -->
    <dubbo:registry address="zookeeper://127.0.0.1:2181" register="false"/>

    <!--定义通信协议与端口（用dubbo协议再30880端口暴露服务）-->
    <dubbo:protocol name="dubbo" port="30880"/>
    <dubbo:annotation package="com.lhw.provider.service"/>

    <!-- 服务提供者缺省配置，可以指定超时时间、自定义异常处理过滤器等等，具体的看到官方文档 -->
    <dubbo:provider filter="dubboExceptionFilter,-exception" timeout="50000"/>

</beans>